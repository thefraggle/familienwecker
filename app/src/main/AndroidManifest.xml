<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Berechtigungen für den Wecker -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Alarm nach Geräteneustart wiederherstellen -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <!-- Telemetriedaten senden (auch für Firebase benötigt) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".FamWakeApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@null"
        android:fullBackupContent="@null"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_short"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.Familienwecker">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:label="@string/app_name_short"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep Linking für Familien-Einladungen und E-Mail-Verifizierung (App Links) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                
                <data android:scheme="https" android:host="familienwecker.de" android:pathPrefix="/join/" />
                <data android:scheme="https" android:host="familienwecker.de" android:pathPrefix="/auth/verify-email" />
                <data android:scheme="https" android:host="familienwecker.de" android:pathPrefix="/verify-email" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.screens.RingingActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout"
            android:theme="@style/Theme.Familienwecker" />

        <receiver
            android:name=".alarm.AlarmReceiver"
            android:exported="false" />

        <!-- Alarm nach Neustart wiederherstellen (auch vor erstem Unlock via LOCKED_BOOT_COMPLETED) -->
        <receiver
            android:name=".alarm.BootReceiver"
            android:exported="false"
            android:directBootAware="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- DST & Systemzeit-Absicherung: Wecker bei Zeit-/Zeitzonenwechsel sofort neu terminieren -->
        <receiver
            android:name=".alarm.TimeChangeReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- TelemetryDeck: anonyme, datenschutzkonforme Nutzungsanalyse -->
        <meta-data
            android:name="com.telemetrydeck.appID"
            android:value="65B4CF62-F147-42B5-9B7A-14CF0ADF949D" />

        <!-- FCM: Empfängt Push-Notifications und verwaltet Token-Lifecycle -->
        <service
            android:name=".FamWakeMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
            
    </application>

</manifest>